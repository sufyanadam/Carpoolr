<p>You're all set!</p>

<%= render 'shared/waiting_stats', :resource => @driver %>

<p>
  Hit the refresh button at least every <%= distance_of_time_in_words Rider::MAX_LAST_SEEN_AT %> to avoid being marked as stale. You will also see if any riders might be on their way
</p>

<%= render 'navigation' %>
